# Токен бота от @BotFather
TELEGRAM_TOKEN=
# 32 байта в urlsafe base64: python -c "import base64,os;print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
SESSION_KEY=
ADMIN_PASSWORD=change-me
DEBUG=false
